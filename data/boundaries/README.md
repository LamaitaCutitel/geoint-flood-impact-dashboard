# Boundaries

Optional small AOI boundary files can be placed here during local experiments.

Do not commit sensitive data, large rasters, or generated outputs. The first
version uses the built-in Galati bounding box preset and custom numeric bounding
box inputs in the Streamlit sidebar.
